development:
  sessions:
    default:
      database: gastos_desarrollo
      hosts:
        - localhost
      options:
        safe: true

test:
  sessions:
    default:
      database: gastos_prueba
      hosts:
        - localhost
      options:
        safe: true

production:
  sessions:
    default:
      database: gastos_produccion
      hosts:
        - localhost
